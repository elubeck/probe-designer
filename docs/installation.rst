============
Installation
============

At the command line::

    $ easy_install probe_designer

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv probe_designer
    $ pip install probe_designer